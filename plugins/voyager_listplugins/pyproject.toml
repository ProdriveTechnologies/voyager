[project]
name = "voyager-plugin-list"
version = "1.0.0"
description = "Lists installed voyager plugins."
requires-python = "==3.10.*"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["voyager_plugins"]
